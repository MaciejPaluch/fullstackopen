sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: writes note and clicks Save button
    Browser->>Server: POST /notes (note as JSON)
    activate Server
    Server->>Browser: new note as JSON
    deactivate Server

    Browser->>Browser: JavaScript updates page (DOM)